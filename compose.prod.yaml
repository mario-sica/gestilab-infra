# Profilo prod. Scritto e versionato, MAI ESEGUITO finché non ci sarà un
# dominio registrato e un VPS (vedi "Prerequisiti a spesa" nel backlog MVP
# di gestilab-app — fuori dalla fase attuale). Nessuna differenza
# strutturale rispetto a compose.local-prod.yaml (che resta in gestilab-app,
# è uno strumento di sviluppo/verifica locale, non un artefatto di deploy):
# stessi servizi, stesso routing. Cambiano solo BASE_DOMAIN, l'emittente del
# certificato (Let's Encrypt DNS-01 invece di mkcert) e l'irrobustimento
# operativo per un ambiente reale (restart policy, limiti di risorse).
#
# Questo file è un OVERLAY: la sua base (compose.yaml, la definizione dei
# servizi condivisa da ogni profilo) vive in gestilab-app, un repository
# diverso. Vedi README.md per la procedura di deploy completa — in breve:
#   git clone --branch <tag-di-release> https://github.com/mario-sica/gestilab-app.git app
#   docker compose -f app/compose.yaml -f compose.prod.yaml up -d
# eseguito dalla cartella di questo repository (così Compose legge .env da
# qui, dove vivono i segreti di produzione — mai versionati, vedi
# .env.example). Nessuna modifica al codice applicativo per passare da
# local-prod a prod: solo BASE_DOMAIN, il token del provider DNS e i DSN dei
# servizi esterni in .env.

x-restart-policy: &restart-policy unless-stopped

services:
  proxy:
    image: traefik:v3
    restart: *restart-policy
    mem_limit: 256m
    cpus: 0.5
    ports:
      - "80:80"
      - "443:443"
    env_file:
      - .env
    command:
      - --certificatesresolvers.letsencrypt.acme.email=${ACME_EMAIL}
      - --certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json
      - --certificatesresolvers.letsencrypt.acme.dnschallenge=true
      - --certificatesresolvers.letsencrypt.acme.dnschallenge.provider=${DNS_PROVIDER}
    volumes:
      - ./docker/traefik/traefik.prod.yml:/etc/traefik/traefik.yml:ro,z
      - letsencrypt:/letsencrypt
      - /var/run/docker.sock:/var/run/docker.sock:ro,z
    healthcheck:
      test: ["CMD", "traefik", "healthcheck", "--ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    depends_on:
      web:
        condition: service_healthy

  web:
    image: gestilab-web-prod
    restart: *restart-policy
    mem_limit: 512m
    cpus: 1.0
    environment:
      BASE_DOMAIN: ${BASE_DOMAIN}
    labels:
      - traefik.enable=true
      - traefik.http.routers.web.rule=Host(`dellaquila.${BASE_DOMAIN}`) || Host(`demo.${BASE_DOMAIN}`) || Host(`app.${BASE_DOMAIN}`) || Host(`console.${BASE_DOMAIN}`)
      - traefik.http.routers.web.entrypoints=websecure
      - traefik.http.routers.web.tls=true
      - traefik.http.routers.web.tls.certresolver=letsencrypt
      - traefik.http.services.web.loadbalancer.server.port=3000

  api:
    image: gestilab-api-prod
    restart: *restart-policy
    mem_limit: 512m
    cpus: 1.0
    environment:
      BASE_DOMAIN: ${BASE_DOMAIN}

  db:
    restart: *restart-policy
    mem_limit: 1024m
    cpus: 1.0

  redis:
    restart: *restart-policy
    mem_limit: 256m
    cpus: 0.5

  storage:
    restart: *restart-policy
    mem_limit: 512m
    cpus: 1.0

  mailpit:
    restart: *restart-policy
    mem_limit: 128m
    cpus: 0.25

  glitchtip:
    restart: *restart-policy
    mem_limit: 1024m
    cpus: 1.0

volumes:
  letsencrypt:
